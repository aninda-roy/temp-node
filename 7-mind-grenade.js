const num1 = 5
const num2 = 10;

function addValues(){
    console.log(`the sum is : ${num1 + num2}`)
}

addValues()
//as we are executing the function in this module, importing the module in anyfile will also invoke the function automatically